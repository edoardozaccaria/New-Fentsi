// Zustand store entry point.
// Each domain slice goes in its own file (e.g., useAuthStore.ts, useCartStore.ts).
// Install Zustand when needed: npm install zustand

// Example:
// export { useAuthStore } from './useAuthStore'
