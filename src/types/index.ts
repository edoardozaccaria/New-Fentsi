// Global shared TypeScript types and interfaces.
// Domain-specific types go in their own files (e.g., user.types.ts, payment.types.ts).

export type Nullable<T> = T | null
export type Optional<T> = T | undefined
